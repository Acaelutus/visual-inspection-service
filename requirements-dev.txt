# Зависимости для разработки и CI.
# НЕ ставятся в production Docker образ.
# Установка: pip install -r requirements-dev.txt

pytest==8.1.1         # фреймворк для тестов
pytest-asyncio==0.23.6  # поддержка async тестов
httpx==0.27.0         # HTTP клиент (нужен TestClient в FastAPI)
ruff==0.3.0           # линтер (быстрый, на Rust)
