# Зависимости только для запуска тестов в CI.
# НЕ включает ultralytics (PyTorch ~600MB) и mlflow — они мокаются в conftest.py.
# Установка: pip install -r requirements-test.txt -r requirements-dev.txt

fastapi==0.128.8
uvicorn[standard]==0.39.0
python-multipart==0.0.20
pydantic==2.13.4
opencv-python-headless==4.13.0.92
numpy==2.0.2
celery==5.3.6
redis==5.0.1
prometheus-fastapi-instrumentator==7.1.0
prometheus-client==0.19.0
